server:
  port: 8085

spring:
  application:
    name: file-service

# TODO (P4, sprint S7): cấu hình MinIO
# minio:
#   endpoint: http://localhost:9000
#   access-key: sse
#   secret-key: sse_dev_minio

logging:
  level:
    com.sse.file: DEBUG
