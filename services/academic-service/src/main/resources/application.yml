server:
  port: 8082

spring:
  application:
    name: academic-service

  datasource:
    url: jdbc:postgresql://localhost:5442/academic_db
    username: sse
    password: sse_dev
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

logging:
  level:
    com.sse.academic: DEBUG
