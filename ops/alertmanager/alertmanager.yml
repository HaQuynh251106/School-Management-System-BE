route:
  receiver: operations
  group_by: [alertname, severity]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: operations

# Alerts remain visible in Alertmanager/Grafana. Production must add an email or
# webhook integration generated from a secret manager; never commit its secret.
